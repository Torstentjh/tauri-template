on:
  push:
    branches:
      - release

jobs:
  buildForWindows:
    runs-on: windows-latest
    steps:
      - name: 设置 Tauri Action
        uses: tauri-apps/tauri-action@v0
      - name: 安装依赖📦
        run: pnpm install
      - name: 构建🔨
        run: pnpm run build
